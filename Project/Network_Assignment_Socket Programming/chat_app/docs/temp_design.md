1. 서버는 몇 번 포트에서 실행할 것인가?
   포트: 5000
2. 서버는 127.0.0.1로 열 것인가, 0.0.0.0으로 열 것인가?
   서버 HOST: 0.0.0.0
3. 클라이언트는 서버 IP를 어디서 입력받을 것인가?
   클라이언트 입력 IP: GUI에서 입력
4. 메시지는 단순 문자열인가, JSON인가?
   메시지 형식: JSON
5. 메시지 끝은 어떻게 구분할 것인가?
   끝: \n
6. 클라이언트 닉네임은 언제 서버에 전달할 것인가?
   닉네임 전달: 접속 직후 join 메시지 전송
7. 서버는 클라이언트 목록을 list로 관리할 것인가, dict로 관리할 것인가?
   서버 클라이언트 관리: dict
8. GUI 수신 메시지는 Queue로 전달할 것인가?
   GUI 메시지 전달: Queue 사용
9. 서버가 꺼졌을 때 클라이언트 화면에 어떻게 표시할 것인가?
   서버 종료: "서버와 연결이 끊어졌습니다" 표시
10. 클라이언트가 종료되면 서버는 어떻게 제거할 것인가?
    클라이언트 종료: clients에서 제거

```
같은 컴퓨터에서 서버 1개, 클라이언트 2개를 실행한다.
클라이언트 A가 메시지를 보내면 클라이언트 B 화면에 표시된다.
```
