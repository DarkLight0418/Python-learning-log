# 함수 내부 및 외부에서 정의한 변수, 객체 식별 번호

n = 1
def put_id():
  x = 1
  print(f'id(x) = {id(x)}')
  
  
print(f'id(1) = {id(1)}')
print(f'id(n) = {id(n)}')
put_id()


'''
(내가 틀린 부분 gpt가 검토해서 수정)

파이썬에서 정수 1은 CPython의 정수 캐싱 정책에 의해
미리 생성되어 재사용되는 객체임.

따라서 함수 내부(x)와 외부(n)에서 1을 사용하더라도
모두 동일한 정수 객체를 참조함.

함수 종료 시 지역 변수 x 자체는 소멸되지만,
정수 객체 1은 다른 참조(n)가 남아 있으므로 소멸되지 않음.
'''
